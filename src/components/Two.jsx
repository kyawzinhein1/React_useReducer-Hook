import React from "react";


const Two = (props) => {
  return (
    <div>
      <ul>
        <li>{props.name}</li>
        <li>{props.address}</li>
      </ul>
    </div>
  );
};

export default Two;
